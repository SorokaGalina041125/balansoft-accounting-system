# Balansoft — Система расчёта зарплаты

Учебный проект «Бухгалтерия» для **ООО "Лукоморье"**: расчёт зарплаты по тарифной сетке, учёт НДФЛ, налоговых вычетов и особых условий (алименты, исполнительные производства). Данные — 10 сотрудников-персонажей из сказок А. С. Пушкина.

Проект выполнен в рамках задания по модулям и пакетам Python: основной запуск через `main.py`, функции `calculate_salary()` и `get_employees()` в пакете `application`, опциональная работа с БД и JSON.

---

## Требования

- **Python 3.13+**
- Для базового запуска достаточно стандартной библиотеки; данные берутся из JSON.
- Для расширенного вывода (таблицы) — пакет **rich**.
- Для опциональной работы с БД — **SQLAlchemy**, **asyncpg**, **python-dotenv** (см. раздел «Зависимости»).

---

## Установка и быстрый старт

```bash
# Клонирование / переход в каталог проекта
cd Balansoft

# Рекомендуется: виртуальное окружение
python -m venv venv
venv\Scripts\activate   # Windows
# source venv/bin/activate   # Linux/macOS

# Минимальная установка (для main.py и dirty_main.py хватает только Python)
# Для красивого вывода и тестов:
pip install -r requirements.txt

# Запуск основного сценария
python main.py
```

После запуска выводится текущая дата, список сотрудников из выбранного источника (📊 БД / 📄 JSON / 🧪 Тест) и результат расчёта зарплаты для одного сотрудника (по умолчанию — Баба Яга).

---

## Режимы запуска

### 1. Базовый режим — `main.py`

Соответствует формулировке задания: импорт `datetime`, вызов `get_employees()` и `calculate_salary()` в `if __name__ == '__main__'`, вывод текущей даты и результатов.

```bash
python main.py
```

**Пример вывода:**

- Заголовок и текущая дата  
- Источник данных: `📄 JSON` (или `[JSON]` в консоли без Unicode)  
- Количество сотрудников и список: ФИО — должность  
- Результат расчёта зарплаты: для кого выполнен расчёт и сумма к выплате  

Работает **без установки** SQLAlchemy и PostgreSQL; данные читаются из `data/employees.json`.

---

### 2. Вариант с `import *` — `dirty_main.py`

Необязательная часть задания: импорт через `from application.salary import *` и `from application.db.people import *`, тот же сценарий вызова и вывода.

```bash
python dirty_main.py
```

Используется только для демонстрации синтаксиса `import *`; в реальных проектах такой стиль не рекомендуется.

---

### 3. Расширенный режим — `run.py`

Дополнительный скрипт с форматированием вывода (таблицы и подсветка через **rich**). Требует установки пакета `rich`.

```bash
pip install rich
python run.py
```

Вывод: таблица сотрудников (ФИО, должность, оклад) и детализированный расчёт зарплаты для выбранного сотрудника.

---

## Структура проекта

Balansoft/
├── main.py                 # Точка входа по заданию
├── dirty_main.py           # Вариант с import *
├── run.py                  # Расширенный вывод (rich)
├── requirements.txt        # Зависимости
├── .env.example            # Шаблон переменных окружения (БД)
├── .gitignore
├── README.md
├── database_schema.md       # Описание схемы БД
│
├── application/            # Пакет из задания
│   ├──`__init__.py`
│   ├── salary.py           # calculate_salary() — расчёт зарплаты
│   └── db/
│       ├──`__init__.py`
│       └── people.py       # get_employees() — получение списка сотрудников
│
├── data/
│   └── employees.json      # 10 сотрудников (сказочные персонажи), тарифная сетка
│
├── database/               # Опциональный слой БД (не обязателен для запуска)
│   ├──`__init__.py`
│   ├── models.py           # SQLAlchemy-модели (Employee и др.)
│   ├── session.py          # Подключение к PostgreSQL
│   └── seed.py             # Загрузка тестовых данных в БД
│
├── utils/
│   ├──`__init__.py`
│   ├── formatters.py       # Форматирование (валюта и т.п.)
│   └── salary_calculator.py # Логика расчёта НДФЛ и удержаний
│
└── tests/
    └── test_basic.py       # Тесты get_employees() и calculate_salary()

**main.py**
Импорт модуля datetime, вызов функций `get_employees()` и `calculate_salary()`, вывод текущей даты и результатов расчёта.

**application/salary.py**
Содержит функцию `calculate_salary(employee_id=3)`. Она загружает данные о сотруднике из JSON-файла, выполняет расчёт заработной платы через утилиту `utils.salary_calculator` и возвращает словарь с информацией об окладе, НДФЛ, удержаниях и итоговой сумме к выплате.

**application/db/people.py**
Содержит функцию `get_employees(use_db_if_available=True)`. Эта функция отдаёт приоритет данным из JSON-файла, при необходимости может использовать данные из базы данных (опционально). В случае любых ошибок функция возвращает тестовые данные. При каждом вызове она выводит в консоль источник данных (📊 БД, 📄 JSON или 🧪 Тест).

**data/employees.json**
Файл-справочник компании. Содержит информацию об отделах, тарифной сетке (6 разрядов) и данные 10 сотрудников со всеми полями, необходимыми для расчёта зарплаты.

**database/**
Директория с опциональной реализацией работы с базой данных PostgreSQL. Включает модели данных, настройку сессии и сиды для заполнения базы. Если библиотека SQLAlchemy не установлена, проект будет работать только с данными из JSON-файлов.

**utils/salary_calculator.py**
Модуль для расчёта зарплаты. Вычисляет НДФЛ (ставка 13%), применяет налоговый вычет и учитывает особые удержания, такие как алименты или удержания по исполнительному производству.

## Источники данных сотрудников

Функция `get_employees(use_db_if_available=True)` сама выбирает источник:

1. **БД (опционально)** — если передан `use_db_if_available=True`, настроен `DATABASE_URL` и доступны SQLAlchemy/драйвер БД. При любой ошибке подключения или запроса используется следующий источник.
2. **JSON (основной)** — файл `data/employees.json`. Используется по умолчанию при отсутствии или недоступности БД.
3. **Тестовые данные** — минимальный список из одного «тестового» сотрудника, если JSON отсутствует или повреждён.

Формат списка сотрудников единый для всех источников (поля `id`, `full_name`, `position`, `base_salary`, `tax_deduction`, `special_conditions` и др.). В консоль выводится метка источника: 📊 БД / 📄 JSON / 🧪 Тест (или [БД] / [JSON] / [Тест] в средах без поддержки эмодзи).

Проект **полностью работоспособен без PostgreSQL и без установки sqlalchemy/asyncpg**: в этом случае всегда используется JSON (или тестовые данные).

---

## Расчёт зарплаты

- **Тарифно-повременная система:** 6 разрядов, коэффициенты к базовому окладу (см. `data/employees.json`, секция `tariff_grid`).
- **НДФЛ 13%** от суммы с учётом налогового вычета (`tax_deduction`).
- **Особые условия (удержания из суммы после НДФЛ):**
  - исполнительное производство — до 50%;
  - алименты — 25% (1 ребёнок), 33% (2 детей и более).

Расчёт выполняется в `utils/salary_calculator.py`; `application/salary.py` вызывает его и возвращает словарь с полями вроде `employee_name`, `base_salary`, `ndfl`, `special_deduction`, `net_salary`.

По умолчанию `calculate_salary()` считает зарплату для сотрудника с `id=3` (Баба Яга, исполнительное производство 30%).

---

## Конфигурация (опционально)

Для использования БД скопируйте шаблон и задайте строку подключения:

```bash
cp .env.example .env
# В .env указать:
# DATABASE_URL=postgresql://user:password@localhost:5432/balansoft_db
```

Без файла `.env` или без установленных зависимостей БД приложение работает с JSON.

---

## База данных PostgreSQL

Проект может использовать PostgreSQL для хранения сотрудников. БД **опциональна**; при её отсутствии данные читаются из `data/employees.json`.

### 1. Создание базы данных

База `balansoft_db` должна быть создана **до** запуска миграций и сида. Команды для создания:

```bash
# Windows (psql в PATH)
psql -U postgres -c "CREATE DATABASE balansoft_db;"

# Linux / macOS
sudo -u postgres psql -c "CREATE DATABASE balansoft_db;"

# Или через createdb
createdb -U postgres balansoft_db
```

### 2. Установка зависимостей для работы с БД

```bash
# Активировать виртуальное окружение (если используется)
venv\Scripts\activate   # Windows
# source venv/bin/activate   # Linux/macOS

# Установить все зависимости (включая SQLAlchemy, psycopg2, alembic)
pip install -r requirements.txt
```

### 3. Настройка подключения

Создать файл `.env` в корне проекта:

```bash
cp .env.example .env
```

Указать в `.env` реальные учётные данные PostgreSQL:

DATABASE_URL=postgresql://user:password@localhost:5432/balansoft_db

### 4. Создание таблиц

```bash
alembic upgrade head
```

Либо через Python (создаёт таблицы по моделям):

```bash
python -c "from database.seed import create_tables; create_tables()"
```

### 5. Заполнение базы данных

Загрузка данных из `data/employees.json` в БД через SQLAlchemy ORM:

```bash
python -m database.seed
```

Очистка таблиц и повторное заполнение:

```bash
python -m database.seed --clear
```

После успешного выполнения `get_employees()` при наличии настроенной БД будет брать данные из PostgreSQL (вывод: 📊 БД).

---

## Зависимости (requirements.txt)

```text
# Для основного и расширенного вывода
rich>=13.7.0

# Опционально: слой БД (database/)
sqlalchemy>=2.0.25
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0
alembic>=1.13.0

# Разработка
pytest>=7.4.0
black>=23.0.0
```

Минимальный запуск `main.py` и `dirty_main.py` возможен **без установки пакетов** (данные из JSON).

---

## Тестирование

```bash
pip install pytest
python -m pytest tests/test_basic.py -v
```

Проверяются:

- `get_employees()` возвращает 10 сотрудников и ожидаемое первое ФИО;
- `calculate_salary(employee_id=3)` возвращает ожидаемые значения НДФЛ, удержаний и суммы к выплате для Бабы Яги.
